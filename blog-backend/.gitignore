node_modules
.local-persist
.teeny
.tmp.*.json
.dev.vars
.prod.vars
# Migrations are generated by `teeny generate`/`teeny deploy` and pulled from
# the live deployment; keep them out of git to avoid drift.
migrations
